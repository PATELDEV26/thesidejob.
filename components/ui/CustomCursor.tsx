"use client";

// Custom visual cursor removed — browser default cursor is used everywhere.
// This component is kept as a no-op to avoid breaking layout.tsx imports.

export default function CustomCursor() {
    return null;
}
